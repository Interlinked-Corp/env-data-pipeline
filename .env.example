# Environment Variables for Environmental Data Pipeline

# OpenWeatherMap API Configuration
OPENWEATHER_API_KEY=your_openweather_api_key_here
OPENWEATHER_ENV=dev

# AWS Configuration: Uses mounted ~/.aws/credentials directory
# Make sure you have run: aws configure
# (No environment variables needed for development)

# RabbitMQ Configuration (for message queue)
RABBITMQ_DEFAULT_USER=pipeline_user
RABBITMQ_DEFAULT_PASS=pipeline_dev_pass